pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }

    // Define the Flutter plugin and any other required plugins
    plugins {
        id "com.android.application" version "7.4.2" apply false // Update AGP version
        id "org.jetbrains.kotlin.android" version "1.8.22" apply false // Kotlin plugin
        id "com.google.gms.google-services" version "4.3.13" apply false // Firebase/Google services plugin
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "YourProjectName" // Replace with your project name
include(":app")
